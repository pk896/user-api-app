<section class="auth-page mini">
  <h1 class="page-title">📬 Check your email</h1>

  <div class="card">
    <p>
      We’ve sent a password reset link to
      <strong><%= maskedEmail || 'your email' %></strong>
      (if it exists in our system).
    </p>
    <ul class="tips">
      <li>It can take a minute to arrive.</li>
      <li>Check spam/junk if you don't see it.</li>
      <li>The link expires in 1 hour.</li>
    </ul>

    <div class="actions">
      <a class="btn primary" href="/business/login">Back to login</a>
      <a class="btn" href="/business/password/forgot">Use a different email</a>
    </div>
  </div>
</section>

<style nonce="<%= nonce %>">
  .auth-page.mini { max-width:360px;margin:.9rem auto;padding:.8rem .7rem;}
  .page-title{text-align:center;font-size:1.02rem;font-weight:800;margin:0 0 .4rem;}
  .card{border-radius:10px;border:1px solid #dbeafe;padding:.8rem;background:#fff;}
  .tips{margin:.6rem 0 .8rem .9rem;font-size:.8rem;color:#4b5563;}
  .actions{display:flex;flex-wrap:wrap;gap:.5rem;}
  .btn{border-radius:8px;border:1px solid #d1d5db;padding:.4rem .7rem;font-size:.78rem;font-weight:700;text-decoration:none;text-align:center;flex:1;}
  .btn.primary{background:#2563eb;border-color:#2563eb;color:#fff;}
</style>
