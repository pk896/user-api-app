<!-- views/users-login.ejs -->
<div class="professional-login">
  <!-- Background Decoration -->
  <div class="background-decoration">
    <div class="decoration-circle circle-1"></div>
    <div class="decoration-circle circle-2"></div>
    <div class="decoration-circle circle-3"></div>
  </div>

  <!-- Main Container -->
  <div class="login-container">
    <!-- Left Side - Brand & Features -->
    <div class="login-hero">
      <div class="hero-content">
        <div class="brand-header">
          <div class="brand-logo">
            <svg viewBox="0 0 24 24" width="32" height="32">
              <path fill="currentColor" d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm-2 15l-5-5 1.41-1.41L10 14.17l7.59-7.59L19 8l-9 9z"/>
            </svg>
          </div>
          <h1 class="brand-name">🌍 Unicoporate.com</h1>
        </div>
        
        <div class="hero-welcome">
          <h2>Welcome Back</h2>
          <p>Sign in to access your account and continue your journey with our platform.</p>
        </div>

        <div class="hero-features">
          <div class="feature-item">
            <div class="feature-icon">
              <svg viewBox="0 0 24 24" width="24" height="24">
                <path fill="currentColor" d="M20 6h-4V4c0-1.11-.89-2-2-2h-4c-1.11 0-2 .89-2 2v2H4c-1.11 0-1.99.89-1.99 2L2 19c0 1.11.89 2 2 2h16c1.11 0 2-.89 2-2V8c0-1.11-.89-2-2-2zm-6 0h-4V4h4v2z"/>
              </svg>
            </div>
            <div class="feature-content">
              <h4>Secure Access</h4>
              <p>Your data is protected with enterprise-grade security</p>
            </div>
          </div>

          <div class="feature-item">
            <div class="feature-icon">
              <svg viewBox="0 0 24 24" width="24" height="24">
                <path fill="currentColor" d="M3 13h8V3H3v10zm0 8h8v-6H3v6zm10 0h8V11h-8v10zm0-18v6h8V3h-8z"/>
              </svg>
            </div>
            <div class="feature-content">
              <h4>Dashboard</h4>
              <p>Access your personalized dashboard</p>
            </div>
          </div>

          <div class="feature-item">
            <div class="feature-icon">
              <svg viewBox="0 0 24 24" width="24" height="24">
                <path fill="currentColor" d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
              </svg>
            </div>
            <div class="feature-content">
              <h4>Community</h4>
              <p>Connect with other users and grow together</p>
            </div>
          </div>
        </div>

        <div class="hero-stats">
          <div class="stat-item">
            <div class="stat-number">100K+</div>
            <div class="stat-label">Active Users</div>
          </div>
          <div class="stat-item">
            <div class="stat-number">99.9%</div>
            <div class="stat-label">Uptime</div>
          </div>
        </div>

        <div class="hero-footer">
          <p>Don't have an account?</p>
          <a href="/users/signup" class="btn-signup">
            <svg viewBox="0 0 24 24" width="18" height="18">
              <path fill="currentColor" d="M19 13h-6v6h-2v-6H5v-2h6V5h2v6h6v2z"/>
            </svg>
            Create Account
          </a>
        </div>
      </div>
    </div>

    <!-- Right Side - Login Form -->
    <div class="login-form-container">
      <div class="form-header">
        <h2>Sign In to Your Account</h2>
        <p class="form-subtitle">Enter your credentials to access the platform</p>
      </div>

      <!-- Flash Messages -->
      <% (success || []).forEach(msg => { %>
        <div class="flash-message success">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <polyline points="20 6 9 17 4 12"></polyline>
          </svg>
          <span><%= msg %></span>
        </div>
      <% }) %>

      <% (error || []).forEach(msg => { %>
        <div class="flash-message error">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <circle cx="12" cy="12" r="10"></circle>
            <line x1="15" y1="9" x2="9" y2="15"></line>
            <line x1="9" y1="9" x2="15" y2="15"></line>
          </svg>
          <span><%= msg %></span>
        </div>
      <% }) %>

      <form action="/users/login" method="POST" class="login-form" id="loginForm">
        <!-- Username/Email Field -->
        <div class="form-group">
          <label for="login-username" class="form-label">
            Username or Email
            <span class="required">*</span>
          </label>
          <div class="input-with-icon">
            <svg class="input-icon" viewBox="0 0 24 24" width="20" height="20">
              <path fill="currentColor" d="M20 4H4c-1.1 0-1.99.9-1.99 2L2 18c0 1.1.9 2 2 2h16c1.1 0 2-.9 2-2V6c0-1.1-.9-2-2-2zm0 14H4V8l8 5 8-5v10zm-8-7L4 6h16l-8 5z"/>
            </svg>
            <input
              id="login-username"
              type="text"
              name="loginId"
              placeholder="Enter your username or email"
              autocomplete="username email"
              required
              class="form-input"
            />
          </div>
          <div class="form-hint">Enter your registered username or email address</div>
        </div>

        <!-- Password Field -->
        <div class="form-group">
          <label for="login-password" class="form-label">
            Password
            <span class="required">*</span>
          </label>
          <div class="password-container">
            <div class="input-with-icon">
              <svg class="input-icon" viewBox="0 0 24 24" width="20" height="20">
                <path fill="currentColor" d="M18 8h-1V6c0-2.76-2.24-5-5-5S7 3.24 7 6v2H6c-1.1 0-2 .9-2 2v10c0 1.1.9 2 2 2h12c1.1 0 2-.9 2-2V10c0-1.1-.9-2-2-2zm-6 9c-1.1 0-2-.9-2-2s.9-2 2-2 2 .9 2 2-.9 2-2 2zm3.1-9H8.9V6c0-1.71 1.39-3.1 3.1-3.1 1.71 0 3.1 1.39 3.1 3.1v2z"/>
              </svg>
              <input
                id="login-password"
                type="password"
                name="password"
                placeholder="Enter your password"
                autocomplete="current-password"
                required
                minlength="6"
                class="form-input"
              />
            </div>
            <button type="button" class="toggle-password" id="showPasswordBtn" aria-label="Toggle password visibility">
              <svg class="eye-icon" viewBox="0 0 24 24" width="20" height="20">
                <path fill="currentColor" d="M12 4.5C7 4.5 2.73 7.61 1 12c1.73 4.39 6 7.5 11 7.5s9.27-3.11 11-7.5c-1.73-4.39-6-7.5-11-7.5zM12 17c-2.76 0-5-2.24-5-5s2.24-5 5-5 5 2.24 5 5-2.24 5-5 5zm0-8c-1.66 0-3 1.34-3 3s1.34 3 3 3 3-1.34 3-3-1.34-3-3-3z"/>
              </svg>
            </button>
          </div>
          <div class="form-actions-row">
            <label class="checkbox-label remember-me">
              <input type="checkbox" id="login-remember" name="remember" value="1" />
              <span class="checkbox-custom"></span>
              <span class="checkbox-text">Remember this device</span>
            </label>
            <a href="/users/password/forgot" class="forgot-password">
              Forgot password?
            </a>
          </div>
        </div>

        <!-- Submit Button -->
        <button type="submit" class="btn btn-primary">
          <svg viewBox="0 0 24 24" width="20" height="20">
            <path fill="currentColor" d="M10 17v-3H3v-4h7V7l5 5-5 5z"/>
          </svg>
          Sign In
        </button>

        <!-- Security Notice -->
        <div class="security-notice">
          <svg viewBox="0 0 24 24" width="18" height="18">
            <path fill="currentColor" d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4z"/>
          </svg>
          <span>Your connection is secure. All data is encrypted.</span>
        </div>

        <!-- Social Login Options -->
        <div class="social-login">
          <div class="divider">
            <span>Or continue with</span>
          </div>
          
          <div class="social-buttons">
            <a href="/auth/google" class="social-btn google">
              <svg viewBox="0 0 24 24" width="20" height="20">
                <path fill="#4285F4" d="M22.56 12.25c0-.78-.07-1.53-.2-2.25H12v4.26h5.92c-.26 1.37-1.04 2.53-2.21 3.31v2.77h3.57c2.08-1.92 3.28-4.74 3.28-8.09z"/>
                <path fill="#34A853" d="M12 23c2.97 0 5.46-.98 7.28-2.66l-3.57-2.77c-.98.66-2.23 1.06-3.71 1.06-2.86 0-5.29-1.93-6.16-4.53H2.18v2.84C3.99 20.53 7.7 23 12 23z"/>
                <path fill="#FBBC05" d="M5.84 14.09c-.22-.66-.35-1.36-.35-2.09s.13-1.43.35-2.09V7.07H2.18C1.43 8.55 1 10.22 1 12s.43 3.45 1.18 4.93l2.85-2.22.81-.62z"/>
                <path fill="#EA4335" d="M12 5.38c1.62 0 3.06.56 4.21 1.64l3.15-3.15C17.45 2.09 14.97 1 12 1 7.7 1 3.99 3.47 2.18 7.07l3.66 2.84c.87-2.6 3.3-4.53 6.16-4.53z"/>
              </svg>
              Google
            </a>
          </div>
          
          <p class="social-note">
            If you signed up with Google, use this button to log in.
            Your Google email will be used for authentication.
          </p>
        </div>

        <!-- Demo Access -->
        <div class="demo-section">
          <details class="demo-details">
            <summary class="demo-summary">
              <svg viewBox="0 0 24 24" width="18" height="18">
                <path fill="currentColor" d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm1 15h-2v-2h2v2zm0-4h-2V7h2v6z"/>
              </svg>
              <span>Quick Demo Access</span>
            </summary>
            <div class="demo-content">
              <p class="demo-info">For testing purposes only:</p>
              <div class="demo-credentials">
                <div class="credential">
                  <span>Username:</span>
                  <code>demo_user</code>
                </div>
                <div class="credential">
                  <span>Password:</span>
                  <code>demo123</code>
                </div>
              </div>
              <button type="button" class="demo-btn" id="fillDemoBtn">Fill Demo Credentials</button>
            </div>
          </details>
        </div>
      </form>
    </div>
  </div>
</div>

<style>
  /* Base Styles */
  * {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
    -webkit-tap-highlight-color: transparent;
  }

  body {
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif;
    line-height: 1.5;
    background: #f8fafc;
    color: #1e293b;
  }

  .professional-login {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 20px;
    position: relative;
    overflow: hidden;
  }

  /* Background Decoration */
  .background-decoration {
    position: absolute;
    width: 100%;
    height: 100%;
    z-index: 1;
  }

  .decoration-circle {
    position: absolute;
    border-radius: 50%;
    filter: blur(60px);
    opacity: 0.3;
  }

  .circle-1 {
    width: 400px;
    height: 400px;
    background: #3b82f6;
    top: -150px;
    right: -150px;
  }

  .circle-2 {
    width: 300px;
    height: 300px;
    background: #8b5cf6;
    bottom: -100px;
    left: -100px;
  }

  .circle-3 {
    width: 200px;
    height: 200px;
    background: #10b981;
    top: 40%;
    left: 10%;
  }

  /* Main Container */
  .login-container {
    position: relative;
    z-index: 2;
    display: grid;
    grid-template-columns: 1fr 1fr;
    max-width: 1200px;
    width: 100%;
    min-height: 700px;
    background: white;
    border-radius: 24px;
    overflow: hidden;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.1);
  }

  @media (max-width: 1024px) {
    .login-container {
      grid-template-columns: 1fr;
      min-height: auto;
    }
  }

  /* Hero Section */
  .login-hero {
    background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
    color: white;
    padding: 60px 40px;
    display: flex;
    flex-direction: column;
    justify-content: center;
  }

  .hero-content {
    max-width: 460px;
    margin: 0 auto;
    width: 100%;
  }

  .brand-header {
    display: flex;
    align-items: center;
    gap: 16px;
    margin-bottom: 48px;
  }

  .brand-logo {
    width: 64px;
    height: 64px;
    background: rgba(255, 255, 255, 0.15);
    border-radius: 16px;
    display: flex;
    align-items: center;
    justify-content: center;
    backdrop-filter: blur(10px);
  }

  .brand-logo svg {
    color: white;
  }

  .brand-name {
    font-size: 32px;
    font-weight: 800;
    letter-spacing: -0.5px;
    margin: 0;
    background: linear-gradient(to right, #ffffff, #e2e8f0);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-welcome {
    margin-bottom: 48px;
  }

  .hero-welcome h2 {
    font-size: 32px;
    font-weight: 700;
    margin: 0 0 16px;
    line-height: 1.2;
  }

  .hero-welcome p {
    font-size: 16px;
    opacity: 0.9;
    line-height: 1.6;
    margin: 0;
  }

  /* Hero Features */
  .hero-features {
    margin-bottom: 48px;
  }

  .feature-item {
    display: flex;
    gap: 16px;
    margin-bottom: 24px;
    align-items: flex-start;
  }

  .feature-icon {
    width: 48px;
    height: 48px;
    background: rgba(255, 255, 255, 0.15);
    border-radius: 12px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    backdrop-filter: blur(10px);
  }

  .feature-icon svg {
    color: white;
  }

  .feature-content h4 {
    font-size: 16px;
    font-weight: 600;
    margin: 0 0 4px;
  }

  .feature-content p {
    font-size: 14px;
    opacity: 0.8;
    margin: 0;
    line-height: 1.4;
  }

  /* Hero Stats */
  .hero-stats {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 24px;
    background: rgba(255, 255, 255, 0.15);
    border-radius: 16px;
    padding: 24px;
    backdrop-filter: blur(10px);
    margin-bottom: 48px;
  }

  .stat-item {
    text-align: center;
  }

  .stat-number {
    font-size: 28px;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .stat-label {
    font-size: 14px;
    opacity: 0.9;
  }

  /* Hero Footer */
  .hero-footer {
    text-align: center;
  }

  .hero-footer p {
    font-size: 14px;
    opacity: 0.8;
    margin-bottom: 16px;
  }

  .btn-signup {
    display: inline-flex;
    align-items: center;
    gap: 8px;
    background: rgba(255, 255, 255, 0.15);
    color: white;
    padding: 12px 24px;
    border-radius: 12px;
    text-decoration: none;
    font-weight: 600;
    font-size: 14px;
    border: 1px solid rgba(255, 255, 255, 0.2);
    transition: all 0.3s ease;
    backdrop-filter: blur(10px);
  }

  .btn-signup:hover {
    background: rgba(255, 255, 255, 0.25);
    transform: translateY(-2px);
  }

  .btn-signup svg {
    color: white;
  }

  /* Form Container */
  .login-form-container {
    padding: 60px 40px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    background: white;
  }

  /* Form Header */
  .form-header {
    margin-bottom: 32px;
  }

  .form-header h2 {
    font-size: 28px;
    font-weight: 700;
    color: #0f172a;
    margin: 0 0 8px;
  }

  .form-subtitle {
    font-size: 14px;
    color: #64748b;
    margin: 0;
  }

  /* Flash Messages */
  .flash-message {
    display: flex;
    align-items: center;
    gap: 12px;
    padding: 16px;
    border-radius: 12px;
    margin-bottom: 24px;
    font-size: 14px;
    animation: slideIn 0.3s ease;
  }

  .flash-message.success {
    background: #dcfce7;
    color: #065f46;
    border-left: 4px solid #10b981;
  }

  .flash-message.error {
    background: #fee2e2;
    color: #991b1b;
    border-left: 4px solid #ef4444;
  }

  /* Login Form */
  .login-form {
    max-width: 400px;
    margin: 0 auto;
    width: 100%;
  }

  /* Form Elements */
  .form-group {
    margin-bottom: 24px;
  }

  .form-label {
    display: block;
    margin-bottom: 8px;
    font-weight: 600;
    color: #0f172a;
    font-size: 14px;
  }

  .required {
    color: #ef4444;
  }

  /* Input Fields - CLEAR VISIBILITY */
  .form-input {
    width: 100%;
    padding: 16px 20px 16px 48px;
    font-size: 16px;
    border: 2px solid #e2e8f0;
    border-radius: 12px;
    background: #f8fafc;
    color: #0f172a;
    transition: all 0.3s ease;
    font-weight: 500;
    letter-spacing: 0.3px;
  }

  .form-input:focus {
    outline: none;
    border-color: #3b82f6;
    background: white;
    box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.15);
  }

  .form-input::placeholder {
    color: #94a3b8;
    font-weight: 400;
  }

  /* Input with Icon */
  .input-with-icon {
    position: relative;
  }

  .input-icon {
    position: absolute;
    left: 16px;
    top: 50%;
    transform: translateY(-50%);
    color: #64748b;
    pointer-events: none;
  }

  /* Form Hint */
  .form-hint {
    margin-top: 8px;
    font-size: 12px;
    color: #64748b;
  }

  /* Password Container */
  .password-container {
    position: relative;
  }

  .toggle-password {
    position: absolute;
    right: 16px;
    top: 50%;
    transform: translateY(-50%);
    background: none;
    border: none;
    cursor: pointer;
    padding: 8px;
    border-radius: 8px;
    color: #64748b;
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s ease;
  }

  .toggle-password:hover {
    color: #0f172a;
    background: #f1f5f9;
  }

  /* Form Actions Row */
  .form-actions-row {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 12px;
    flex-wrap: wrap;
    gap: 12px;
  }

  .checkbox-label {
    display: flex;
    align-items: center;
    gap: 8px;
    cursor: pointer;
    user-select: none;
  }

  .checkbox-label input {
    display: none;
  }

  .checkbox-custom {
    width: 18px;
    height: 18px;
    border: 2px solid #cbd5e1;
    border-radius: 4px;
    position: relative;
    transition: all 0.2s ease;
    flex-shrink: 0;
  }

  .checkbox-label input:checked + .checkbox-custom {
    background: #3b82f6;
    border-color: #3b82f6;
  }

  .checkbox-label input:checked + .checkbox-custom::after {
    content: '';
    position: absolute;
    left: 4px;
    top: 1px;
    width: 6px;
    height: 10px;
    border: solid white;
    border-width: 0 2px 2px 0;
    transform: rotate(45deg);
  }

  .checkbox-text {
    font-size: 14px;
    color: #475569;
    font-weight: 500;
  }

  .forgot-password {
    font-size: 14px;
    color: #3b82f6;
    text-decoration: none;
    font-weight: 600;
    transition: all 0.2s ease;
  }

  .forgot-password:hover {
    text-decoration: underline;
    color: #2563eb;
  }

  /* Submit Button */
  .btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    padding: 18px 32px;
    font-size: 16px;
    font-weight: 600;
    border-radius: 12px;
    border: none;
    cursor: pointer;
    transition: all 0.3s ease;
    text-decoration: none;
    width: 100%;
  }

  .btn-primary {
    background: linear-gradient(135deg, #3b82f6 0%, #2563eb 100%);
    color: white;
    box-shadow: 0 4px 15px rgba(59, 130, 246, 0.25);
    margin-top: 8px;
  }

  .btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(59, 130, 246, 0.35);
  }

  .btn-primary:active {
    transform: translateY(0);
  }

  /* Security Notice */
  .security-notice {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    font-size: 13px;
    color: #64748b;
    padding: 16px;
    background: #f1f5f9;
    border-radius: 12px;
    margin: 24px 0;
  }

  .security-notice svg {
    color: #10b981;
    flex-shrink: 0;
  }

  /* Social Login */
  .social-login {
    margin: 24px 0;
  }

  .divider {
    display: flex;
    align-items: center;
    margin: 24px 0;
    color: #64748b;
    font-size: 14px;
  }

  .divider::before,
  .divider::after {
    content: '';
    flex: 1;
    height: 1px;
    background: #e2e8f0;
  }

  .divider span {
    padding: 0 16px;
  }

  .social-buttons {
    display: grid;
    grid-template-columns: 1fr;
    gap: 12px;
    margin-bottom: 16px;
  }

  .social-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    padding: 14px 16px;
    border: 2px solid #e2e8f0;
    border-radius: 12px;
    background: white;
    color: #0f172a;
    font-weight: 600;
    text-decoration: none;
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .social-btn:hover {
    border-color: #cbd5e1;
    background: #f8fafc;
    transform: translateY(-2px);
  }

  .social-note {
    font-size: 12px;
    color: #64748b;
    text-align: center;
    line-height: 1.5;
    margin: 0;
  }

  /* Demo Section */
  .demo-section {
    margin-top: 24px;
  }

  .demo-details {
    background: #f1f5f9;
    border: 2px solid #e2e8f0;
    border-radius: 12px;
    overflow: hidden;
  }

  .demo-summary {
    list-style: none;
    padding: 16px 20px;
    cursor: pointer;
    display: flex;
    align-items: center;
    gap: 12px;
    font-size: 14px;
    font-weight: 600;
    color: #475569;
    transition: background 0.2s ease;
  }

  .demo-summary:hover {
    background: #e2e8f0;
  }

  .demo-summary::-webkit-details-marker {
    display: none;
  }

  .demo-summary svg {
    color: #8b5cf6;
    flex-shrink: 0;
  }

  .demo-content {
    padding: 20px;
    border-top: 2px solid #e2e8f0;
    display: flex;
    flex-direction: column;
    gap: 16px;
  }

  .demo-info {
    font-size: 13px;
    color: #64748b;
    margin: 0;
  }

  .demo-credentials {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .credential {
    display: flex;
    justify-content: space-between;
    align-items: center;
    font-size: 13px;
  }

  .credential span {
    color: #64748b;
    font-weight: 500;
  }

  .credential code {
    background: white;
    padding: 8px 16px;
    border-radius: 8px;
    border: 1px solid #e2e8f0;
    font-family: 'SF Mono', Monaco, Consolas, monospace;
    color: #0f172a;
    font-weight: 600;
  }

  .demo-btn {
    width: 100%;
    padding: 12px;
    background: #8b5cf6;
    color: white;
    border: none;
    border-radius: 8px;
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.2s ease;
  }

  .demo-btn:hover {
    background: #7c3aed;
    transform: translateY(-2px);
  }

  /* Animations */
  @keyframes slideIn {
    from {
      opacity: 0;
      transform: translateY(-10px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .professional-login {
      padding: 16px;
    }
    
    .login-container {
      border-radius: 20px;
    }
    
    .login-hero {
      padding: 40px 24px;
    }
    
    .login-form-container {
      padding: 40px 24px;
    }
    
    .brand-header {
      margin-bottom: 32px;
    }
    
    .brand-name {
      font-size: 28px;
    }
    
    .hero-welcome h2 {
      font-size: 28px;
    }
    
    .form-header h2 {
      font-size: 24px;
    }
    
    .form-input {
      padding: 14px 20px 14px 44px;
      font-size: 15px;
    }
    
    .btn {
      padding: 16px 24px;
    }
  }

  @media (max-width: 480px) {
    .login-container {
      border-radius: 16px;
    }
    
    .login-hero {
      padding: 32px 20px;
    }
    
    .login-form-container {
      padding: 32px 20px;
    }
    
    .brand-logo {
      width: 56px;
      height: 56px;
    }
    
    .brand-name {
      font-size: 24px;
    }
    
    .hero-welcome h2 {
      font-size: 24px;
    }
    
    .hero-welcome p {
      font-size: 14px;
    }
    
    .feature-content h4 {
      font-size: 15px;
    }
    
    .feature-content p {
      font-size: 13px;
    }
    
    .stat-number {
      font-size: 24px;
    }
    
    .form-header h2 {
      font-size: 22px;
    }
    
    .form-input {
      padding: 12px 16px 12px 40px;
    }
    
    .form-actions-row {
      flex-direction: column;
      align-items: flex-start;
      gap: 16px;
    }
    
    .forgot-password {
      align-self: flex-end;
    }
  }

  /* Dark Theme */
  .dark-theme .professional-login {
    background: #0f172a;
  }
  
  .dark-theme .login-container {
    background: #1e293b;
  }
  
  .dark-theme .login-form-container {
    background: #1e293b;
  }
  
  .dark-theme .form-header h2 {
    color: #f1f5f9;
  }
  
  .dark-theme .form-subtitle {
    color: #94a3b8;
  }
  
  .dark-theme .form-input {
    background: #334155;
    border-color: #475569;
    color: #f1f5f9;
  }
  
  .dark-theme .form-input:focus {
    border-color: #60a5fa;
    background: #475569;
  }
  
  .dark-theme .form-input::placeholder {
    color: #94a3b8;
  }
  
  .dark-theme .form-label {
    color: #e2e8f0;
  }
  
  .dark-theme .form-hint {
    color: #94a3b8;
  }
  
  .dark-theme .checkbox-text {
    color: #cbd5e1;
  }
  
  .dark-theme .checkbox-custom {
    border-color: #64748b;
  }
  
  .dark-theme .security-notice {
    background: #334155;
    color: #cbd5e1;
  }
  
  .dark-theme .social-btn {
    background: #334155;
    border-color: #475569;
    color: #e2e8f0;
  }
  
  .dark-theme .social-btn:hover {
    background: #475569;
    border-color: #64748b;
  }
  
  .dark-theme .divider::before,
  .dark-theme .divider::after {
    background: #475569;
  }
  
  .dark-theme .demo-details {
    background: #334155;
    border-color: #475569;
  }
  
  .dark-theme .demo-summary {
    color: #cbd5e1;
  }
  
  .dark-theme .demo-summary:hover {
    background: #475569;
  }
  
  .dark-theme .credential code {
    background: #1e293b;
    border-color: #475569;
    color: #f1f5f9;
  }
</style>

<script nonce="<%= typeof nonce !== 'undefined' ? nonce : '' %>">
  (function() {
    'use strict';
    
    // Toggle password visibility
    const passwordInput = document.getElementById('login-password');
    const showPasswordBtn = document.getElementById('showPasswordBtn');
    
    if (passwordInput && showPasswordBtn) {
      showPasswordBtn.addEventListener('click', function() {
        const isPassword = passwordInput.type === 'password';
        passwordInput.type = isPassword ? 'text' : 'password';
        
        // Update eye icon
        const eyeIcon = this.querySelector('.eye-icon');
        if (eyeIcon) {
          if (isPassword) {
            // Eye with slash (visible)
            eyeIcon.innerHTML = '<path fill="currentColor" d="M12 7c2.76 0 5 2.24 5 5 0 .65-.13 1.26-.36 1.83l2.92 2.92c1.51-1.26 2.7-2.89 3.43-4.75-1.73-4.39-6-7.5-11-7.5-1.4 0-2.74.25-3.98.7l2.16 2.16C10.74 7.13 11.35 7 12 7zM2 4.27l2.28 2.28.46.46C3.08 8.3 1.78 10.02 1 12c1.73 4.39 6 7.5 11 7.5 1.55 0 3.03-.3 4.38-.84l.42.42L19.73 22 21 20.73 3.27 3 2 4.27zM7.53 9.8l1.55 1.55c-.05.21-.08.43-.08.65 0 1.66 1.34 3 3 3 .22 0 .44-.03.65-.08l1.55 1.55c-.67.33-1.41.53-2.2.53-2.76 0-5-2.24-5-5 0-.79.2-1.53.53-2.2zm4.31-.78l3.15 3.15.02-.16c0-1.66-1.34-3-3-3l-.17.01z"/>';
          } else {
            // Regular eye (hidden)
            eyeIcon.innerHTML = '<path fill="currentColor" d="M12 4.5C7 4.5 2.73 7.61 1 12c1.73 4.39 6 7.5 11 7.5s9.27-3.11 11-7.5c-1.73-4.39-6-7.5-11-7.5zM12 17c-2.76 0-5-2.24-5-5s2.24-5 5-5 5 2.24 5 5-2.24 5-5 5zm0-8c-1.66 0-3 1.34-3 3s1.34 3 3 3 3-1.34 3-3-1.34-3-3-3z"/>';
          }
        }
      });
    }
    
    // Demo credentials filler
    const fillDemoBtn = document.getElementById('fillDemoBtn');
    if (fillDemoBtn) {
      fillDemoBtn.addEventListener('click', function() {
        const usernameInput = document.getElementById('login-username');
        const passwordInput = document.getElementById('login-password');
        const rememberCheckbox = document.getElementById('login-remember');
        
        if (usernameInput) {
          usernameInput.value = 'demo_user';
          usernameInput.focus();
          // Add visual feedback
          usernameInput.style.borderColor = '#8b5cf6';
          usernameInput.style.boxShadow = '0 0 0 4px rgba(139, 92, 246, 0.15)';
        }
        
        if (passwordInput) {
          passwordInput.value = 'demo123';
          passwordInput.style.borderColor = '#8b5cf6';
          passwordInput.style.boxShadow = '0 0 0 4px rgba(139, 92, 246, 0.15)';
        }
        
        if (rememberCheckbox) {
          rememberCheckbox.checked = true;
        }
        
        // Show success message
        const successMsg = document.createElement('div');
        successMsg.className = 'flash-message success';
        successMsg.innerHTML = `
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
            <polyline points="20 6 9 17 4 12"></polyline>
          </svg>
          <span>Demo credentials filled. Click Sign In to continue.</span>
        `;
        
        const form = document.querySelector('.login-form');
        if (form) {
          const existingMsg = form.previousElementSibling;
          if (existingMsg && existingMsg.classList.contains('flash-message')) {
            existingMsg.remove();
          }
          form.parentNode.insertBefore(successMsg, form);
          
          setTimeout(() => {
            if (successMsg.parentNode) {
              successMsg.style.opacity = '0';
              successMsg.style.transform = 'translateY(-10px)';
              successMsg.style.transition = 'all 0.3s ease';
              setTimeout(() => successMsg.remove(), 300);
            }
          }, 3000);
        }
      });
    }
    
    // Form submission
    const loginForm = document.getElementById('loginForm');
    if (loginForm) {
      loginForm.addEventListener('submit', function(e) {
        const submitBtn = this.querySelector('.btn-primary');
        const usernameInput = document.getElementById('login-username');
        const passwordInput = document.getElementById('login-password');
        
        // Basic validation
        let isValid = true;
        
        if (!usernameInput.value.trim()) {
          isValid = false;
          usernameInput.style.borderColor = '#ef4444';
          usernameInput.style.boxShadow = '0 0 0 4px rgba(239, 68, 68, 0.15)';
        }
        
        if (!passwordInput.value.trim() || passwordInput.value.length < 6) {
          isValid = false;
          passwordInput.style.borderColor = '#ef4444';
          passwordInput.style.boxShadow = '0 0 0 4px rgba(239, 68, 68, 0.15)';
        }
        
        if (!isValid) {
          e.preventDefault();
          
          // Show error message
          const errorMsg = document.createElement('div');
          errorMsg.className = 'flash-message error';
          errorMsg.innerHTML = `
            <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <circle cx="12" cy="12" r="10"></circle>
              <line x1="15" y1="9" x2="9" y2="15"></line>
              <line x1="9" y1="9" x2="15" y2="15"></line>
            </svg>
            <span>Please fill in all required fields correctly. Password must be at least 6 characters.</span>
          `;
          
          const form = document.querySelector('.login-form');
          if (form) {
            const existingMsg = form.previousElementSibling;
            if (existingMsg && existingMsg.classList.contains('flash-message')) {
              existingMsg.remove();
            }
            form.parentNode.insertBefore(errorMsg, form);
            
            setTimeout(() => {
              if (errorMsg.parentNode) {
                errorMsg.style.opacity = '0';
                errorMsg.style.transform = 'translateY(-10px)';
                errorMsg.style.transition = 'all 0.3s ease';
                setTimeout(() => errorMsg.remove(), 300);
              }
            }, 5000);
          }
          
          return false;
        }
        
        // Show loading state
        if (submitBtn) {
          submitBtn.disabled = true;
          submitBtn.style.opacity = '0.7';
          submitBtn.innerHTML = `
            <svg class="spinner" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
              <path d="M12 2v4M12 18v4M4.93 4.93l2.83 2.83M16.24 16.24l2.83 2.83M2 12h4M18 12h4M4.93 19.07l2.83-2.83M16.24 7.76l2.83-2.83"/>
            </svg>
            Signing in...
          `;
          
          // Add spinner animation
          const style = document.createElement('style');
          style.textContent = `
            @keyframes spin {
              from { transform: rotate(0deg); }
              to { transform: rotate(360deg); }
            }
            .spinner {
              animation: spin 1s linear infinite;
            }
          `;
          document.head.appendChild(style);
        }
      });
    }
    
    // Input focus effects
    const inputs = document.querySelectorAll('.form-input');
    inputs.forEach(input => {
      input.addEventListener('focus', function() {
        this.style.transform = 'translateY(-1px)';
      });
      
      input.addEventListener('blur', function() {
        this.style.transform = 'translateY(0)';
        if (this.value.trim()) {
          this.style.borderColor = '#10b981';
          this.style.boxShadow = '0 0 0 4px rgba(16, 185, 129, 0.15)';
        } else {
          this.style.borderColor = '';
          this.style.boxShadow = '';
        }
      });
      
      input.addEventListener('input', function() {
        if (this.value.trim()) {
          this.style.borderColor = '#3b82f6';
          this.style.boxShadow = '0 0 0 4px rgba(59, 130, 246, 0.15)';
        }
      });
    });
    
    // Auto-focus username
    setTimeout(() => {
      const usernameInput = document.getElementById('login-username');
      if (usernameInput && !usernameInput.value) {
        usernameInput.focus();
      }
    }, 300);
    
    // Remember me functionality
    const rememberCheckbox = document.getElementById('login-remember');
    if (rememberCheckbox) {
      // Load saved username
      try {
        const savedUsername = localStorage.getItem('login:username');
        if (savedUsername && usernameInput) {
          usernameInput.value = savedUsername;
          rememberCheckbox.checked = true;
        }
      } catch (error) {
        console.warn('Could not load saved username:', error);
      }
      
      // Save username on change
      if (usernameInput) {
        usernameInput.addEventListener('input', function() {
          if (rememberCheckbox.checked && this.value.trim()) {
            try {
              localStorage.setItem('login:username', this.value.trim());
            } catch (error) {
              console.warn('Could not save username:', error);
            }
          }
        });
      }
      
      rememberCheckbox.addEventListener('change', function() {
        if (!this.checked) {
          try {
            localStorage.removeItem('login:username');
          } catch (error) {
            console.warn('Could not remove saved username:', error);
          }
        }
      });
    }
    
    // Responsive adjustments
    function adjustLayout() {
      const width = window.innerWidth;
      const container = document.querySelector('.login-container');
      
      if (width < 640) {
        container.style.minHeight = 'auto';
      } else {
        container.style.minHeight = '700px';
      }
    }
    
    adjustLayout();
    window.addEventListener('resize', adjustLayout);
    
  })();
</script>