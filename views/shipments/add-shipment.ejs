<!-- views/shipments/add-shipment.ejs -->
<section class="dashboard-home add-shipment">
  <h1 class="page-title">➕ Add New Shipment</h1>
  <p class="subtitle">Create and dispatch a new shipment record linked to one of your products.</p>

  <!-- ✅ Flash Messages -->
  <% if (success && success.length > 0) { %>
    <div class="flash flash-success" id="flashMessage">✅ <%= success %></div>
  <% } %>
  <% if (error && error.length > 0) { %>
    <div class="flash flash-error" id="flashMessage">❌ <%= error %></div>
  <% } %>

  <!-- 🧾 Shipment Form -->
  <form action="/shipments/add" method="POST" class="shipment-form">
    <!-- Order ID -->
    <div class="form-group">
      <label for="orderId">Order ID <span class="required">*</span></label>
      <input type="text" id="orderId" name="orderId" required placeholder="e.g. ORD-20251009-001" />
    </div>

    <!-- Product -->
    <div class="form-group">
      <label for="productId">Product <span class="required">*</span></label>
      <select id="productId" name="productId" required>
        <option value="">Select product</option>
        <% products.forEach(p => { %>
          <option value="<%= p._id %>"><%= p.name %></option>
        <% }) %>
      </select>
    </div>

    <!-- Buyer Info -->
    <div class="form-group-grid">
      <div>
        <label for="buyerName">Buyer Name</label>
        <input type="text" id="buyerName" name="buyerName" placeholder="John Doe" />
      </div>
      <div>
        <label for="buyerEmail">Buyer Email</label>
        <input type="email" id="buyerEmail" name="buyerEmail" placeholder="buyer@example.com" />
      </div>
    </div>

    <!-- Address -->
    <div class="form-group">
      <label for="address">Shipping Address</label>
      <textarea id="address" name="address" rows="3" placeholder="123 Main Street, City, Country"></textarea>
    </div>

    <!-- Tracking Number -->
    <div class="form-group">
      <label for="trackingNumber">Tracking Number (optional)</label>
      <input type="text" id="trackingNumber" name="trackingNumber" placeholder="e.g. TRK-20251009-2341" />
    </div>

    <!-- Status -->
    <div class="form-group">
      <label for="status">Shipment Status</label>
      <select id="status" name="status">
        <option value="Pending" selected>Pending</option>
        <option value="In Transit">In Transit</option>
        <option value="Delivered">Delivered</option>
        <option value="Cancelled">Cancelled</option>
      </select>
    </div>

    <!-- Submit -->
    <div class="form-actions">
      <button type="submit" class="btn btn-primary">🚀 Create Shipment</button>
      <a href="/shipments" class="btn btn-secondary">⬅ Back to Shipments</a>
    </div>
  </form>
</section>

<!-- 🌈 Scoped Styles -->
<style>
.dashboard-home {
  padding: 1.5rem;
  max-width: 700px;
  margin: 0 auto;
}
.page-title {
  text-align: center;
  font-size: 2rem;
  font-weight: 700;
  margin-bottom: 0.3rem;
}
.subtitle {
  text-align: center;
  color: #6b7280;
  margin-bottom: 1.5rem;
}

/* ✨ Flash messages */
.flash {
  padding: 0.8rem 1rem;
  border-radius: 8px;
  margin-bottom: 1rem;
  font-weight: 500;
  text-align: center;
  animation: fadeIn 0.4s ease;
}
.flash-success {
  background: #dcfce7;
  color: #166534;
  border: 1px solid #bbf7d0;
}
.flash-error {
  background: #fee2e2;
  color: #991b1b;
  border: 1px solid #fecaca;
}
.flash.fade-out {
  opacity: 0;
  transform: translateY(-10px);
  transition: opacity 0.6s, transform 0.6s;
}
@keyframes fadeIn {
  from { opacity: 0; transform: translateY(-5px); }
  to { opacity: 1; transform: translateY(0); }
}

/* 🧾 Form styling */
.shipment-form {
  background: var(--form-bg, #fff);
  border-radius: 12px;
  box-shadow: 0 2px 6px rgba(0,0,0,0.08);
  padding: 1.5rem;
}
.form-group {
  margin-bottom: 1rem;
}
.form-group-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 1rem;
  margin-bottom: 1rem;
}
label {
  display: block;
  font-weight: 600;
  margin-bottom: 0.3rem;
}
input, select, textarea {
  width: 100%;
  padding: 0.6rem;
  border: 1px solid #d1d5db;
  border-radius: 6px;
  font-size: 1rem;
}
textarea {
  resize: vertical;
}
.required {
  color: #dc2626;
}
.form-actions {
  display: flex;
  justify-content: space-between;
  margin-top: 1.5rem;
}

/* 🎨 Buttons */
.btn {
  display: inline-block;
  font-weight: 600;
  text-decoration: none;
  border: none;
  cursor: pointer;
  border-radius: 6px;
  padding: 0.6rem 1.2rem;
  transition: background 0.2s ease;
}
.btn-primary {
  background: #2563eb;
  color: #fff;
}
.btn-primary:hover {
  background: #1d4ed8;
}
.btn-secondary {
  background: #e5e7eb;
  color: #111827;
}
.btn-secondary:hover {
  background: #d1d5db;
}

/* 🌙 Dark mode adjustments */
.dark-theme .shipment-form {
  background: #1f2937;
  color: #f3f4f6;
}
.dark-theme input, 
.dark-theme select, 
.dark-theme textarea {
  background: #111827;
  color: #f9fafb;
  border-color: #374151;
}
.dark-theme .btn-secondary {
  background: #374151;
  color: #e5e7eb;
}
.dark-theme .flash-success { background: #064e3b; color: #d1fae5; }
.dark-theme .flash-error { background: #7f1d1d; color: #fee2e2; }
</style>

<!-- ✅ CSP-Safe Inline Script -->
<script nonce="<%= nonce %>">
document.addEventListener("DOMContentLoaded", () => {
  const flash = document.getElementById("flashMessage");
  if (flash) {
    setTimeout(() => {
      flash.classList.add("fade-out");
      setTimeout(() => flash.remove(), 600);
    }, 5000);
  }
});
</script>
