<!--view/businessverify-pending.ejs-->
<section class="verify-page">
  <div class="verify-card">
    <h1>📧 Verify your email</h1>

    <p class="intro">
      Hi <strong><%= business.name %></strong>,
    </p>

    <p class="text">
      We’ve created your <strong><%= business.role.toUpperCase() %></strong> business account,
      but you need to verify your email before accessing your dashboard.
    </p>

    <div class="email-box">
      <div class="label">Verification email sent to</div>
      <div class="value"><%= business.email %></div>
    </div>

    <p class="text small">
      Please check your inbox (and spam / promotions) for an email with the subject
      <strong>"Verify your business email"</strong>, then click the button in that email.
    </p>

    <form action="/business/verify/resend" method="post" class="actions">
      <button type="submit" class="btn primary">🔁 Resend verification email</button>
      <a href="/business/logout" class="btn light">🚪 Logout</a>
    </form>

    <p class="hint">
      Already verified? After clicking the link in your email, you’ll be redirected to the correct dashboard automatically.
    </p>
  </div>
</section>

<style nonce="<%= nonce %>">
.verify-page {
  min-height: calc(100vh - 80px);
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 1.5rem;
}

.verify-card {
  max-width: 480px;
  width: 100%;
  background: #ffffff;
  border-radius: 1rem;
  padding: 1.5rem 1.8rem;
  box-shadow: 0 15px 40px rgba(15, 23, 42, 0.12);
  border: 1px solid rgba(148, 163, 184, 0.4);
}

.dark-theme .verify-card {
  background: #020617;
  border-color: #1f2937;
}

.verify-card h1 {
  font-size: 1.4rem;
  margin-bottom: .7rem;
  font-weight: 800;
  text-align: center;
}

.intro {
  margin-bottom: .25rem;
  text-align: center;
}

.text {
  margin: .25rem 0;
  font-size: .9rem;
  color: #4b5563;
}

.dark-theme .text {
  color: #e5e7eb;
}

.text.small {
  font-size: .84rem;
}

.email-box {
  margin: .8rem 0;
  padding: .6rem .75rem;
  border-radius: .6rem;
  background: #eff6ff;
  border: 1px solid #bfdbfe;
}

.dark-theme .email-box {
  background: #0f172a;
  border-color: #1d4ed8;
}

.email-box .label {
  font-size: .75rem;
  text-transform: uppercase;
  letter-spacing: .06em;
  color: #6b7280;
}

.dark-theme .email-box .label {
  color: #9ca3af;
}

.email-box .value {
  font-size: .95rem;
  font-weight: 700;
  margin-top: .1rem;
}

.actions {
  display: flex;
  flex-wrap: wrap;
  gap: .6rem;
  margin: 1rem 0 .4rem;
  justify-content: center;
}

.hint {
  font-size: .78rem;
  text-align: center;
  color: #6b7280;
}

.dark-theme .hint {
  color: #9ca3af;
}

.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: .3rem;
  padding: .45rem .9rem;
  border-radius: .7rem;
  border: 1px solid #e5e7eb;
  background: #f9fafb;
  color: #111827;
  font-weight: 700;
  font-size: .86rem;
  text-decoration: none;
  cursor: pointer;
}
.btn.primary {
  background: #2563eb;
  border-color: #1d4ed8;
  color: #fff;
}
.btn.light {
  background: #f3f4f6;
  border-color: #e5e7eb;
}
</style>

