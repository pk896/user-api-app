<!--dashboard.ejs-->
<section>
  <h1>Dashboard</h1>
  <p style="color:#555;">Welcome back, <%= user.name %>! Here’s a quick overview:</p>

  <div class="dashboard-stats" style="display:grid; grid-template-columns:repeat(auto-fit,minmax(150px,1fr)); gap:1rem;">
    <div class="stat-card">
      <strong>Projects:</strong> <%= stats.projects %>
    </div>
    <div class="stat-card">
      <strong>Tasks Completed:</strong> <%= stats.tasksCompleted %>
    </div>
    <div class="stat-card">
      <strong>Messages:</strong> <%= stats.messages %>
    </div>
    <div class="stat-card">
      <strong>Notifications:</strong> <%= stats.notifications %>
    </div>
    <div class="stat-card">
      <strong>Followers:</strong> <%= stats.followers %>
    </div>
    <div class="stat-card">
      <strong>Following:</strong> <%= stats.following %>
    </div>
    <div class="stat-card">
      <strong>Last Login:</strong> <%= stats.lastLogin %>
    </div>
    <div class="stat-card">
      <strong>Reputation:</strong> <%= stats.reputation %>
    </div>
  </div>

  <p style="margin-top:1rem;">
    Use the navigation bar to explore more features.
  </p>
</section>
