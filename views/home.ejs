<!-- views/home.ejs -->
<%
const pageStyles = `
  /* ✅ Brand Color System */
  :root {
    --blue: #2563EB;
    --blue-dark: #1d4ed8;
    --purple: #7C3AED;
    --purple-dark: #6d28d9;
    --green: #22C55E;
    --green-dark: #16a34a;
    --black: #0F172A;
    
    --gray-50: #f8fafc;
    --gray-100: #f1f5f9;
    --gray-200: #e2e8f0;
    --gray-300: #cbd5e1;
    --gray-400: #94a3b8;
    --gray-500: #64748b;
    --gray-600: #475569;
    --gray-700: #334155;
    --gray-800: #1e293b;
    --gray-900: #0f172a;
    
    --radius-sm: 0.375rem;
    --radius-md: 0.5rem;
    --radius-lg: 0.75rem;
    --radius-xl: 1rem;
    
    --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
    --shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1);
    --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
    --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
    
    --transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
  }

  .dark-theme {
    --bg-primary: var(--black);
    --bg-secondary: var(--gray-800);
    --text-primary: var(--gray-100);
    --text-secondary: var(--gray-300);
    --border: var(--gray-700);
  }

  :root {
    --bg-primary: var(--gray-50);
    --bg-secondary: white;
    --text-primary: var(--gray-900);
    --text-secondary: var(--gray-600);
    --border: var(--gray-200);
  }

  /* ✅ Home Page Container */
  .home-page {
    max-width: 1100px;
    margin: 0 auto;
    padding: 0 0.75rem;
    color: var(--text-primary);
    background: linear-gradient(135deg, var(--green) 0%, #16a34a 100%);
    border-radius: var(--radius-xl);
    overflow: hidden;
  }

  .home-content {
    background: var(--bg-primary);
    margin: 2px;
    border-radius: calc(var(--radius-xl) - 2px);
    padding: 1.5rem;
  }

  /* ✅ Hero Section */
  .hero {
    text-align: center;
    padding: 1.5rem 0 2rem;
    position: relative;
    overflow: hidden;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: linear-gradient(135deg, var(--blue), var(--purple));
    color: white;
    padding: 0.375rem 0.75rem;
    border-radius: 2rem;
    font-size: 0.75rem;
    font-weight: 600;
    margin-bottom: 1rem;
  }

  .hero h1 {
    font-size: 1.75rem;
    font-weight: 800;
    margin-bottom: 0.75rem;
    background: linear-gradient(135deg, var(--blue), var(--purple));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    line-height: 1.2;
  }

  .hero-subtitle {
    font-size: 0.95rem;
    color: var(--text-secondary);
    max-width: 600px;
    margin: 0 auto 1.5rem;
    line-height: 1.6;
  }

  .hero-buttons {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* ✅ Mission Section */
  .mission {
    background: linear-gradient(135deg, rgba(34, 197, 94, 0.08), rgba(124, 58, 237, 0.05));
    border-radius: var(--radius-lg);
    padding: 1.5rem;
    margin: 2rem 0;
    border: 1px solid var(--border);
  }

  .mission h2 {
    font-size: 1.25rem;
    color: var(--blue);
    margin-bottom: 0.75rem;
    font-weight: 700;
  }

  .mission p {
    color: var(--text-primary);
    line-height: 1.6;
    font-size: 0.9rem;
  }

  .mission strong {
    color: var(--purple);
    font-weight: 600;
  }

  /* ✅ Growth Section */
  .growth {
    margin: 2rem 0;
  }

  .growth h2 {
    text-align: center;
    font-size: 1.25rem;
    color: var(--green);
    margin-bottom: 1.25rem;
    font-weight: 700;
  }

  .growth-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
    gap: 1rem;
  }

  .growth-card {
    background: var(--bg-secondary);
    border-radius: var(--radius-lg);
    padding: 1.25rem;
    border: 1px solid var(--border);
    transition: var(--transition);
  }

  .growth-card:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
    border-color: var(--green);
  }

  .growth-card h3 {
    font-size: 0.95rem;
    color: var(--blue);
    margin-bottom: 0.5rem;
    font-weight: 600;
    display: flex;
    align-items: center;
    gap: 0.375rem;
  }

  .growth-card p {
    font-size: 0.85rem;
    color: var(--text-secondary);
    line-height: 1.5;
  }

  .growth-card strong {
    color: var(--purple);
  }

  /* ✅ Humanity Section */
  .humanity {
    background: linear-gradient(135deg, rgba(124, 58, 237, 0.08), rgba(37, 99, 235, 0.05));
    border-radius: var(--radius-lg);
    padding: 1.5rem;
    margin: 2rem 0;
    border: 1px solid var(--border);
  }

  .humanity h2 {
    font-size: 1.25rem;
    color: var(--purple);
    margin-bottom: 0.75rem;
    font-weight: 700;
  }

  .humanity p {
    color: var(--text-primary);
    line-height: 1.6;
    font-size: 0.9rem;
  }

  /* ✅ CTA Section */
  .cta {
    text-align: center;
    padding: 2rem 1rem;
    background: linear-gradient(135deg, rgba(34, 197, 94, 0.1), rgba(37, 99, 235, 0.05));
    border-radius: var(--radius-lg);
    margin: 2rem 0;
    border: 1px solid var(--border);
  }

  .cta h2 {
    font-size: 1.25rem;
    color: var(--green);
    margin-bottom: 0.75rem;
    font-weight: 700;
  }

  .cta p {
    color: var(--text-secondary);
    font-size: 0.9rem;
    max-width: 600px;
    margin: 0 auto 1.5rem;
    line-height: 1.6;
  }

  .cta-buttons {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  /* ✅ Buttons */
  .btn {
    padding: 0.625rem 1.25rem;
    border-radius: var(--radius-md);
    font-weight: 600;
    font-size: 0.875rem;
    text-decoration: none;
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.375rem;
    transition: var(--transition);
    border: none;
    cursor: pointer;
  }

  .btn-primary {
    background: var(--green);
    color: white;
  }

  .btn-primary:hover {
    background: var(--green-dark);
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .btn-secondary {
    background: var(--blue);
    color: white;
  }

  .btn-secondary:hover {
    background: var(--blue-dark);
    transform: translateY(-2px);
    box-shadow: var(--shadow-md);
  }

  .btn-outline {
    background: transparent;
    border: 1px solid var(--border);
    color: var(--text-primary);
  }

  .btn-outline:hover {
    background: var(--bg-primary);
    border-color: var(--green);
    transform: translateY(-2px);
  }

  /* ✅ Footer */
  .home-footer {
    text-align: center;
    padding: 1.5rem 0;
    border-top: 1px solid var(--border);
    margin-top: 2rem;
  }

  .home-footer p {
    color: var(--text-secondary);
    font-size: 0.8rem;
    font-weight: 500;
  }

  /* ✅ Mobile Optimizations for Hisense U50 Lite */
  @media (max-width: 480px) {
    .home-page {
      margin: 0.5rem;
      padding: 0 0.5rem;
    }
    
    .home-content {
      padding: 1rem;
    }
    
    .hero h1 {
      font-size: 1.5rem;
    }
    
    .hero-subtitle {
      font-size: 0.875rem;
      padding: 0 0.5rem;
    }
    
    .hero-buttons,
    .cta-buttons {
      flex-direction: column;
      gap: 0.5rem;
    }
    
    .btn {
      width: 100%;
      padding: 0.75rem;
    }
    
    .growth-grid {
      grid-template-columns: 1fr;
    }
    
    .growth-card,
    .mission,
    .humanity,
    .cta {
      padding: 1rem;
    }
    
    .mission h2,
    .growth h2,
    .humanity h2,
    .cta h2 {
      font-size: 1.125rem;
    }
  }

  /* ✅ Very Small Screens */
  @media (max-width: 360px) {
    .home-page {
      margin: 0.25rem;
      padding: 0 0.25rem;
    }
    
    .home-content {
      padding: 0.75rem;
    }
    
    .hero h1 {
      font-size: 1.25rem;
    }
    
    .growth-card h3 {
      font-size: 0.875rem;
    }
    
    .growth-card p,
    .mission p,
    .humanity p,
    .cta p {
      font-size: 0.8rem;
    }
  }

  /* ✅ Tablet Optimization */
  @media (min-width: 481px) and (max-width: 768px) {
    .growth-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    
    .hero-buttons,
    .cta-buttons {
      flex-wrap: wrap;
    }
    
    .btn {
      min-width: 140px;
    }
  }

  /* ✅ Animation for Cards */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .growth-card {
    animation: fadeInUp 0.5s ease-out;
  }

  .growth-card:nth-child(2) { animation-delay: 0.1s; }
  .growth-card:nth-child(3) { animation-delay: 0.2s; }
  .growth-card:nth-child(4) { animation-delay: 0.3s; }
`;

const pageScripts = `
  document.addEventListener('DOMContentLoaded', function() {
    // Add scroll animation for cards
    const cards = document.querySelectorAll('.growth-card');
    
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.style.opacity = '1';
          entry.target.style.transform = 'translateY(0)';
        }
      });
    }, { threshold: 0.1 });
    
    cards.forEach(card => {
      card.style.opacity = '0';
      card.style.transform = 'translateY(20px)';
      card.style.transition = 'opacity 0.5s ease, transform 0.5s ease';
      observer.observe(card);
    });
    
    // Smooth scroll for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function(e) {
        e.preventDefault();
        const targetId = this.getAttribute('href');
        if (targetId === '#') return;
        
        const targetElement = document.querySelector(targetId);
        if (targetElement) {
          targetElement.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      });
    });
    
    // Mobile menu toggle (if needed)
    const initMobileMenu = () => {
      const menuToggle = document.querySelector('.menu-toggle');
      if (menuToggle) {
        menuToggle.addEventListener('click', () => {
          const nav = document.querySelector('nav');
          nav.classList.toggle('active');
        });
      }
    };
    
    initMobileMenu();
  });
`;
%>

<!-- ✅ Inject styles -->
<style nonce="<%= nonce %>">
  <%= pageStyles %>
</style>

<!-- ✅ Home Page Content -->
<div class="home-page">
  <div class="home-content">
    <!-- Hero Section -->
    <section class="hero">
      <div class="hero-badge">
        <span>🚀</span>
        <span>Welcome to the Future</span>
      </div>
      <h1>Welcome to <strong>Unicoporate.com</strong></h1>
      <p class="hero-subtitle">
        Empowering people, businesses, and nations — together we build a world where innovation and humanity thrive.
      </p>
      <div class="hero-buttons">
        <a href="/business/signup" class="btn btn-primary">
          <span>🏢</span>
          Join as a Business
        </a>
        <a href="/users/signup" class="btn btn-secondary">
          <span>👤</span>
          Create Personal Account
        </a>
      </div>
    </section>

    <!-- Mission Section -->
    <section class="mission">
      <h2>Our Greater Mission</h2>
      <p>
        At <strong>Unicoporate.com</strong>, our mission goes far beyond commerce.  
        We are a community of visionaries working to take humanity to the <strong>civilization of super power</strong> — 
        where technology, opportunity, and compassion unite to uplift all people.  
        We reform the business world not for profit alone, but for progress, equality, and the betterment of life on Earth.
      </p>
    </section>

    <!-- Growth Section -->
    <section class="growth">
      <h2>Empowering Growth at Every Level</h2>
      <div class="growth-grid">
        <div class="growth-card">
          <h3>🚀 Small Business Uplift</h3>
          <p>
            We help small and micro businesses grow into professional, competitive enterprises through mentorship, visibility, and digital transformation.
          </p>
        </div>
        <div class="growth-card">
          <h3>📚 Business Training</h3>
          <p>
            Our platform offers access to ongoing <strong>business training programs</strong> and development workshops that empower entrepreneurs with real-world knowledge.
          </p>
        </div>
        <div class="growth-card">
          <h3>💰 Capital Support</h3>
          <p>
            We connect qualified entrepreneurs with <strong>investment opportunities, startup funding,</strong> and partnership channels to support sustainable business growth.
          </p>
        </div>
        <div class="growth-card">
          <h3>🤝 Global Collaboration</h3>
          <p>
            Unicoporate.com bridges nations and industries — connecting suppliers, buyers, and innovators to collaborate for global prosperity.
          </p>
        </div>
      </div>
    </section>

    <!-- Humanity Section -->
    <section class="humanity">
      <h2>Building Heaven on Earth</h2>
      <p>
        We believe that true progress lies in balance — business with purpose, wealth with wisdom, and success with compassion.  
        Through every trade, every innovation, and every act of service, we strive to bring <strong>heaven on Earth</strong> — 
        where communities flourish and humanity thrives together in peace, abundance, and creativity.
      </p>
    </section>

    <!-- CTA Section -->
    <section class="cta">
      <h2>Be Part of the Movement</h2>
      <p>
        Whether you're an individual, a startup, or an established company — Unicoporate.com welcomes you to join our mission.  
        Sign up today, grow your business, empower your community, and help build the future of civilization.
      </p>
      <div class="cta-buttons">
        <a href="/business/signup" class="btn btn-primary">
          <span>🚀</span>
          Business Sign Up
        </a>
        <a href="/users/signup" class="btn btn-secondary">
          <span>🙌</span>
          Join as a User
        </a>
        <a href="/contact" class="btn btn-outline">
          <span>💬</span>
          Contact Us
        </a>
      </div>
    </section>

    <!-- Footer -->
    <footer class="home-footer">
      <p>© <%= new Date().getFullYear() %> Unicoporate.com. All rights reserved. Together, we rise.</p>
    </footer>
  </div>
</div>

<!-- ✅ Inject scripts -->
<script nonce="<%= nonce %>">
  <%= pageScripts %>
</script>
