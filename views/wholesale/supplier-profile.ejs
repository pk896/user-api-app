<!--views/wholesale/supplier-profile.ejs-->

<style nonce="<%= typeof nonce !== 'undefined' ? nonce : '' %>">
  .wholesale-supplier-page {
    background:
      radial-gradient(circle at top left, rgba(13, 110, 253, 0.11), transparent 30%),
      radial-gradient(circle at top right, rgba(255, 193, 7, 0.14), transparent 28%),
      linear-gradient(180deg, #f6f9ff 0%, #ffffff 48%, #f8fafc 100%);
    min-height: 100vh;
  }

  .supplier-back-link {
    display: inline-flex;
    align-items: center;
    gap: 0.4rem;
    color: #0d6efd;
    font-weight: 800;
    text-decoration: none;
    border-radius: 999px;
    padding: 0.48rem 0.8rem;
    background: rgba(13, 110, 253, 0.08);
    border: 1px solid rgba(13, 110, 253, 0.12);
    transition: all 0.18s ease;
  }

  .supplier-back-link:hover {
    color: #084298;
    background: rgba(13, 110, 253, 0.13);
    transform: translateX(-2px);
  }

  .supplier-hero-card {
    border: 1px solid rgba(13, 110, 253, 0.12);
    border-radius: 24px;
    overflow: hidden;
    background:
      linear-gradient(135deg, rgba(8, 31, 74, 0.96), rgba(13, 110, 253, 0.92)),
      radial-gradient(circle at top right, rgba(255, 193, 7, 0.35), transparent 30%);
    position: relative;
  }

  .supplier-hero-card::after {
    content: "";
    position: absolute;
    width: 220px;
    height: 220px;
    right: -85px;
    top: -85px;
    border-radius: 999px;
    background: rgba(255, 193, 7, 0.28);
  }

  .supplier-hero-content {
    position: relative;
    z-index: 2;
  }

  .supplier-logo-wrap {
    width: 76px;
    height: 76px;
    border-radius: 999px;
    padding: 4px;
    background: rgba(255, 255, 255, 0.22);
    box-shadow: 0 18px 38px rgba(0, 0, 0, 0.18);
    flex: 0 0 auto;
  }

  .supplier-logo-img,
  .supplier-logo-fallback {
    width: 100%;
    height: 100%;
    border-radius: 999px;
    object-fit: cover;
  }

  .supplier-logo-fallback {
    background: linear-gradient(135deg, #0d6efd, #6f42c1);
    color: #fff;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.45rem;
    font-weight: 900;
  }

  .supplier-hero-title {
    color: #fff;
    letter-spacing: -0.04em;
  }

  .supplier-hero-location {
    color: rgba(255, 255, 255, 0.82);
  }

  .supplier-verified-badge {
    display: inline-flex;
    align-items: center;
    border-radius: 999px;
    padding: 0.35rem 0.65rem;
    font-size: 0.72rem;
    font-weight: 900;
    color: #052e16;
    background: #ffc107;
    box-shadow: 0 12px 24px rgba(255, 193, 7, 0.22);
  }

  .supplier-market-btn {
    border-radius: 999px;
    font-weight: 800;
    box-shadow: 0 10px 24px rgba(0, 0, 0, 0.16);
  }

  .supplier-section-title {
    color: #0f172a;
    letter-spacing: -0.02em;
  }

  .supplier-product-card {
    border: 1px solid rgba(15, 23, 42, 0.08);
    border-radius: 20px;
    background: #fff;
    overflow: hidden;
    transition: transform 0.18s ease, box-shadow 0.18s ease, border-color 0.18s ease;
  }

  .supplier-product-card:hover {
    transform: translateY(-4px);
    border-color: rgba(13, 110, 253, 0.22);
    box-shadow: 0 18px 42px rgba(15, 23, 42, 0.13) !important;
  }

  .supplier-img-wrap {
    background: linear-gradient(135deg, #f8fbff, #eef5ff);
    border-bottom: 1px solid rgba(15, 23, 42, 0.06);
  }

  .supplier-img-wrap img {
    transition: transform 0.2s ease;
  }

  .supplier-product-card:hover .supplier-img-wrap img {
    transform: scale(1.035);
  }

  .supplier-stock-pill {
    position: absolute !important;
    top: 0.55rem !important;
    left: 0.55rem !important;
    right: auto !important;
    bottom: auto !important;
    width: auto !important;
    height: auto !important;
    z-index: 5;
    display: inline-flex !important;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    padding: 0.28rem 0.55rem;
    font-size: 0.68rem;
    line-height: 1;
    font-weight: 800;
    background: rgba(25, 135, 84, 0.92);
    color: #fff;
    box-shadow: 0 8px 18px rgba(25, 135, 84, 0.24);
  }

  .supplier-stock-pill.out {
    background: rgba(108, 117, 125, 0.92);
    box-shadow: 0 8px 18px rgba(108, 117, 125, 0.2);
  }

  .supplier-product-title {
    color: #0f172a;
    line-height: 1.25;
    min-height: 2.45em;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }

  .supplier-category-pill {
    display: inline-flex;
    max-width: 100%;
    border-radius: 999px;
    padding: 0.25rem 0.55rem;
    background: rgba(13, 110, 253, 0.08);
    color: #0d6efd;
    font-size: 0.72rem;
    font-weight: 800;
  }

  .supplier-price-box {
    border-radius: 16px;
    background:
      linear-gradient(135deg, rgba(13, 110, 253, 0.08), rgba(255, 193, 7, 0.12));
    border: 1px solid rgba(13, 110, 253, 0.1);
  }

  .supplier-price {
    color: #071f4a;
    letter-spacing: -0.03em;
  }

  .supplier-meta-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.45rem;
  }

  .supplier-meta-item {
    border-radius: 14px;
    background: #f8fafc;
    border: 1px solid rgba(15, 23, 42, 0.06);
    padding: 0.5rem 0.35rem;
    text-align: center;
    min-width: 0;
  }

  .supplier-meta-label {
    display: block;
    color: #64748b;
    font-size: 0.68rem;
    font-weight: 700;
    line-height: 1.1;
  }

  .supplier-meta-value {
    display: block;
    color: #0f172a;
    font-size: 0.78rem;
    font-weight: 800;
    line-height: 1.2;
    margin-top: 0.15rem;
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
  }

  .supplier-card-btn {
    border-radius: 14px;
    font-weight: 800;
  }

  .supplier-qty-group .input-group-text {
    border-radius: 14px 0 0 14px;
    font-weight: 800;
    color: #0f172a;
    background: #eef5ff;
  }

  .supplier-qty-group .form-control {
    border-radius: 0 14px 14px 0;
    font-weight: 800;
  }

  .supplier-empty-card {
    border-radius: 24px;
    border: 1px dashed rgba(13, 110, 253, 0.25);
    background: #fff;
  }

  @media (max-width: 575.98px) {
    .wholesale-supplier-page {
      padding-left: 0.45rem !important;
      padding-right: 0.45rem !important;
    }

    .supplier-hero-card {
      border-radius: 18px;
    }

    .supplier-logo-wrap {
      width: 62px;
      height: 62px;
    }

    .supplier-hero-title {
      font-size: 1.25rem;
    }

    .supplier-product-card {
      border-radius: 16px;
    }

    .supplier-meta-grid {
      gap: 0.3rem;
    }

    .supplier-meta-item {
      padding: 0.42rem 0.25rem;
      border-radius: 12px;
    }

    .supplier-meta-label {
      font-size: 0.62rem;
    }

    .supplier-meta-value {
      font-size: 0.7rem;
    }

    .supplier-card-btn {
      font-size: 0.75rem;
      padding-left: 0.4rem;
      padding-right: 0.4rem;
    }
  }
</style>

<div class="container-fluid px-2 px-sm-3 px-lg-4 py-3 wholesale-supplier-page">

  <div class="mb-3">
    <a href="/wholesale/suppliers" class="supplier-back-link small">
      ← Back to Suppliers
    </a>
  </div>

  <!-- Supplier hero card -->
  <section class="supplier-hero-card shadow-sm mb-3 mb-lg-4">
    <div class="supplier-hero-content p-3 p-sm-4">
      <div class="d-flex flex-column flex-md-row align-items-md-center justify-content-between gap-3">
        <div class="d-flex align-items-center gap-3 min-w-0">
          <div class="supplier-logo-wrap">
            <% if (supplier.logoUrl) { %>
              <img
                src="<%= supplier.logoUrl %>"
                alt="<%= supplier.name %>"
                class="supplier-logo-img"
                loading="lazy"
                decoding="async"
                onerror="this.onerror=null;this.src='/images/branding/logo-unincorporate.png';"
              >
            <% } else { %>
              <div class="supplier-logo-fallback">
                <%= String(supplier.name || 'S').charAt(0).toUpperCase() %>
              </div>
            <% } %>
          </div>

          <div class="min-w-0">
            <span class="supplier-verified-badge mb-2">
              Verified Supplier
            </span>

            <h1 class="supplier-hero-title h4 fw-bold mb-1 text-truncate">
              <%= supplier.name %>
            </h1>

            <p class="supplier-hero-location small mb-0">
              <%= supplier.city || '' %>
              <%= supplier.country ? ', ' + supplier.country : '' %>
            </p>
          </div>
        </div>

        <div class="d-flex flex-wrap gap-2 justify-content-start justify-content-md-end">
          <a href="/wholesale" class="btn btn-light btn-sm supplier-market-btn">
            Marketplace
          </a>
        </div>
      </div>
    </div>
  </section>

  <!-- Section header -->
  <div class="d-flex justify-content-between align-items-end gap-2 mb-3">
    <div>
      <h2 class="h6 fw-bold supplier-section-title mb-0">
        Wholesale Products
      </h2>

      <p class="text-muted small mb-0">
        <% if (products && products.length) { %>
          <%= products.length %> active product<%= products.length === 1 ? '' : 's' %> from this supplier
        <% } else { %>
          Supplier product catalogue
        <% } %>
      </p>
    </div>
  </div>

  <% if (!products || products.length === 0) { %>
    <div class="card supplier-empty-card shadow-sm">
      <div class="card-body text-center py-5">
        <div
          class="mx-auto mb-3 d-flex align-items-center justify-content-center rounded-circle bg-primary bg-opacity-10 text-primary"
          style="width: 64px; height: 64px;"
        >
          <span class="fw-bold fs-4">0</span>
        </div>

        <h3 class="h6 fw-bold text-primary mb-1">
          No active wholesale products
        </h3>

        <p class="text-muted small mb-0">
          This supplier has not published active wholesale products yet.
        </p>
      </div>
    </div>
  <% } else { %>

    <!--
      Mobile: 2 columns
      Tablet: 3 columns
      Desktop: 4 columns
    -->
    <div class="row row-cols-2 row-cols-md-3 row-cols-lg-4 g-2 g-lg-3">
      <% products.forEach((product) => { %>
        <% const moq = Number(product.minimumOrderQuantity || 1); %>
        <% const available = Number(product.availableQuantity || 0); %>
        <% const leadTime = Number(product.leadTimeDays || 0); %>

        <div class="col">
          <div class="card h-100 supplier-product-card shadow-sm">

            <!-- Image -->
            <a href="/wholesale/products/<%= product._id %>" class="text-decoration-none">
              <div class="ratio ratio-4x3 supplier-img-wrap position-relative">
                <% if (available > 0) { %>
                  <span class="supplier-stock-pill">
                    In stock
                  </span>
                <% } else { %>
                  <span class="supplier-stock-pill out">
                    Sold out
                  </span>
                <% } %>

                <% if (product.imageUrl) { %>
                  <img
                    src="<%= product.imageUrl %>"
                    class="w-100 h-100 object-fit-contain p-2 p-sm-3"
                    alt="<%= product.name %>"
                    loading="lazy"
                    decoding="async"
                    onerror="this.onerror=null;this.src='/images/branding/logo-unincorporate.png';"
                  >
                <% } else { %>
                  <div class="d-flex align-items-center justify-content-center w-100 h-100">
                    <div class="text-center px-2">
                      <div class="fw-bold text-primary small mb-1">
                        No image
                      </div>
                      <div class="text-muted" style="font-size: 0.72rem;">
                        Product preview unavailable
                      </div>
                    </div>
                  </div>
                <% } %>
              </div>
            </a>

            <!-- Body -->
            <div class="card-body p-2 p-sm-3 d-flex flex-column">
              <h3 class="small fw-bold supplier-product-title mb-2">
                <%= product.name %>
              </h3>

              <div class="mb-2">
                <span class="supplier-category-pill text-truncate">
                  <%= product.category || 'Uncategorized' %>
                </span>
              </div>

              <!-- Price -->
              <div class="supplier-price-box p-2 mb-2">
                <span class="d-block text-muted fw-bold" style="font-size: 0.68rem;">
                  Wholesale price
                </span>
                <strong class="supplier-price d-block fs-6">
                  R <%= Number(product.wholesalePrice || 0).toFixed(2) %>
                </strong>
              </div>

              <!-- Product stats -->
              <div class="supplier-meta-grid mb-2">
                <div class="supplier-meta-item">
                  <span class="supplier-meta-label">MOQ</span>
                  <span class="supplier-meta-value"><%= moq %></span>
                </div>

                <div class="supplier-meta-item">
                  <span class="supplier-meta-label">Stock</span>
                  <span class="supplier-meta-value"><%= available %></span>
                </div>

                <div class="supplier-meta-item">
                  <span class="supplier-meta-label">Lead</span>
                  <span class="supplier-meta-value"><%= leadTime %>d</span>
                </div>
              </div>

              <!-- Seller actions -->
              <div class="mt-auto">
                <% if (business && business.role === 'seller') { %>
                  <% if (available > 0) { %>
                    <form method="POST" action="/wholesale/cart/add/<%= product._id %>" class="d-grid gap-2">
                      <div class="input-group input-group-sm supplier-qty-group">
                        <span class="input-group-text px-2">Qty</span>
                        <input
                          type="number"
                          name="quantity"
                          class="form-control"
                          min="<%= moq %>"
                          max="<%= available %>"
                          value="<%= moq %>"
                          required
                        >
                      </div>

                      <button type="submit" class="btn btn-primary btn-sm w-100 supplier-card-btn">
                        Request Supply
                      </button>
                    </form>

                    <a href="/wholesale/products/<%= product._id %>" class="btn btn-outline-primary btn-sm w-100 mt-2 supplier-card-btn">
                      View Details
                    </a>
                  <% } else { %>
                    <button type="button" class="btn btn-outline-secondary btn-sm w-100 supplier-card-btn" disabled>
                      Out of Stock
                    </button>

                    <a href="/wholesale/products/<%= product._id %>" class="btn btn-outline-primary btn-sm w-100 mt-2 supplier-card-btn">
                      View Details
                    </a>
                  <% } %>
                <% } else { %>
                  <a href="/wholesale/products/<%= product._id %>" class="btn btn-primary btn-sm w-100 supplier-card-btn">
                    View Details
                  </a>
                <% } %>
              </div>
            </div>

          </div>
        </div>
      <% }) %>
    </div>
  <% } %>
</div>