<% if (success && success.length) { %>
  <div class="flash flash-success">✅ <%= Array.isArray(success) ? success.join("<br>") : success %></div>
<% } %>
<% if (error && error.length) { %>
  <div class="flash flash-error">❌ <%= Array.isArray(error) ? error.join("<br>") : error %></div>
<% } %>
