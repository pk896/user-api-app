<!-- views/about.ejs -->
<%
const pageStyles = `
  /* ✅ Brand Color System */
  :root {
    --blue: #2563EB;
    --blue-dark: #1d4ed8;
    --purple: #7C3AED;
    --purple-dark: #6d28d9;
    --green: #22C55E;
    --green-dark: #16a34a;
    --black: #0F172A;
    
    --gray-50: #f8fafc;
    --gray-100: #f1f5f9;
    --gray-200: #e2e8f0;
    --gray-300: #cbd5e1;
    --gray-400: #94a3b8;
    --gray-500: #64748b;
    --gray-600: #475569;
    --gray-700: #334155;
    --gray-800: #1e293b;
    --gray-900: #0f172a;
    
    --radius-sm: 0.375rem;
    --radius-md: 0.5rem;
    --radius-lg: 0.75rem;
    --radius-xl: 1rem;
    
    --shadow-sm: 0 1px 2px 0 rgb(0 0 0 / 0.05);
    --shadow-md: 0 4px 6px -1px rgb(0 0 0 / 0.1), 0 2px 4px -2px rgb(0 0 0 / 0.1);
    --shadow-lg: 0 10px 15px -3px rgb(0 0 0 / 0.1), 0 4px 6px -4px rgb(0 0 0 / 0.1);
    
    --transition: all 0.2s ease;
  }

  .dark-theme {
    --bg-primary: var(--black);
    --bg-secondary: var(--gray-800);
    --text-primary: var(--gray-100);
    --text-secondary: var(--gray-300);
    --text-muted: var(--gray-400);
    --border: var(--gray-700);
    --accent-bg: rgba(124, 58, 237, 0.1);
  }

  :root {
    --bg-primary: var(--gray-50);
    --bg-secondary: white;
    --text-primary: var(--gray-900);
    --text-secondary: var(--gray-600);
    --text-muted: var(--gray-500);
    --border: var(--gray-200);
    --accent-bg: rgba(124, 58, 237, 0.05);
  }

  /* ✅ About Page Container */
  .about-page {
    max-width: 800px;
    margin: 0.75rem auto;
    background: linear-gradient(135deg, var(--green) 0%, #16a34a 100%);
    border-radius: var(--radius-xl);
    overflow: hidden;
    box-shadow: var(--shadow-lg);
  }

  .about-content-wrapper {
    background: var(--bg-primary);
    margin: 2px;
    border-radius: calc(var(--radius-xl) - 2px);
    padding: 1.25rem;
  }

  /* ✅ Hero Section */
  .about-hero {
    text-align: center;
    padding: 1rem 0 1.5rem;
    border-bottom: 2px solid var(--border);
    margin-bottom: 1.5rem;
  }

  .about-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: linear-gradient(135deg, var(--blue), var(--purple));
    color: white;
    padding: 0.375rem 0.75rem;
    border-radius: 2rem;
    font-size: 0.75rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
  }

  .about-hero h1 {
    font-size: 1.5rem;
    font-weight: 800;
    margin-bottom: 0.5rem;
    background: linear-gradient(135deg, var(--blue), var(--purple));
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    line-height: 1.2;
  }

  .about-hero p {
    color: var(--text-secondary);
    font-size: 0.9rem;
    line-height: 1.5;
    max-width: 600px;
    margin: 0 auto;
  }

  /* ✅ Content Sections */
  .section {
    margin: 1.5rem 0;
    padding: 1.25rem;
    background: var(--bg-secondary);
    border-radius: var(--radius-lg);
    border: 1px solid var(--border);
    transition: var(--transition);
  }

  .section:hover {
    border-color: var(--green);
    box-shadow: var(--shadow-md);
  }

  .section-header {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    margin-bottom: 0.75rem;
  }

  .section-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 36px;
    height: 36px;
    background: linear-gradient(135deg, var(--green), var(--blue));
    color: white;
    border-radius: var(--radius-md);
    font-size: 0.875rem;
    flex-shrink: 0;
  }

  .section h2 {
    font-size: 1.125rem;
    color: var(--blue);
    font-weight: 700;
    margin: 0;
    flex: 1;
  }

  .section p {
    color: var(--text-primary);
    line-height: 1.6;
    font-size: 0.875rem;
    margin-bottom: 0.5rem;
  }

  .section strong {
    color: var(--purple);
    font-weight: 600;
  }

  /* ✅ Offer List */
  .offer-list {
    list-style: none;
    margin: 0.75rem 0 0;
    padding: 0;
  }

  .offer-item {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    padding: 0.625rem;
    margin-bottom: 0.5rem;
    background: var(--bg-primary);
    border-radius: var(--radius-md);
    border: 1px solid var(--border);
    transition: var(--transition);
  }

  .offer-item:hover {
    border-color: var(--purple);
    transform: translateX(4px);
  }

  .offer-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 32px;
    height: 32px;
    background: linear-gradient(135deg, var(--purple), var(--blue));
    color: white;
    border-radius: var(--radius-sm);
    font-size: 0.875rem;
    flex-shrink: 0;
  }

  .offer-text {
    flex: 1;
  }

  .offer-text strong {
    color: var(--blue);
    font-weight: 600;
  }

  /* ✅ Founders Section */
  .founders-section {
    background: var(--accent-bg);
    border-radius: var(--radius-lg);
    padding: 1.25rem;
    margin: 1.5rem 0;
    border: 1px solid var(--border);
  }

  .founders-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: 1rem;
    margin-top: 1rem;
  }

  .founder-card {
    background: var(--bg-secondary);
    border-radius: var(--radius-lg);
    padding: 1.25rem;
    text-align: center;
    border: 1px solid var(--border);
    transition: var(--transition);
    position: relative;
    overflow: hidden;
  }

  .founder-card:hover {
    transform: translateY(-4px);
    border-color: var(--purple);
    box-shadow: var(--shadow-md);
  }

  .founder-card::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    height: 4px;
    background: linear-gradient(to right, var(--green), var(--blue));
  }

  .founder-photo {
    width: 72px;
    height: 72px;
    border-radius: 50%;
    object-fit: cover;
    margin: 0 auto 0.75rem;
    border: 3px solid var(--bg-secondary);
    box-shadow: var(--shadow-sm);
  }

  .founder-card h3 {
    font-size: 1rem;
    color: var(--blue);
    margin-bottom: 0.25rem;
    font-weight: 700;
  }

  .role {
    font-size: 0.8rem;
    color: var(--green);
    font-weight: 600;
    margin-bottom: 0.5rem;
  }

  .contact {
    font-size: 0.8rem;
    color: var(--text-secondary);
    margin-bottom: 0.75rem;
    line-height: 1.4;
  }

  .social-link {
    display: inline-flex;
    align-items: center;
    gap: 0.375rem;
    background: var(--blue);
    color: white;
    padding: 0.375rem 0.75rem;
    border-radius: var(--radius-sm);
    font-size: 0.75rem;
    font-weight: 600;
    text-decoration: none;
    transition: var(--transition);
  }

  .social-link:hover {
    background: var(--blue-dark);
    transform: translateY(-1px);
  }

  /* ✅ Commitment Section */
  .commitment {
    background: linear-gradient(135deg, rgba(34, 197, 94, 0.1), rgba(37, 99, 235, 0.05));
    border-radius: var(--radius-lg);
    padding: 1.5rem;
    margin: 1.5rem 0;
    border: 1px solid var(--border);
    text-align: center;
  }

  .commitment p {
    font-size: 0.95rem;
    color: var(--text-primary);
    font-weight: 500;
    margin: 0;
  }

  .commitment strong {
    color: var(--green);
  }

  /* ✅ Footer */
  .about-footer {
    text-align: center;
    padding: 1.5rem 0 0;
    margin-top: 1.5rem;
    border-top: 1px solid var(--border);
  }

  .footer-tagline {
    color: var(--text-muted);
    font-size: 0.85rem;
    font-weight: 600;
    line-height: 1.5;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    flex-wrap: wrap;
  }

  .footer-tagline strong {
    color: var(--purple);
  }

  /* ✅ Mobile Optimizations */
  @media (max-width: 480px) {
    .about-page {
      margin: 0.5rem;
    }
    
    .about-content-wrapper {
      padding: 1rem;
    }
    
    .about-hero h1 {
      font-size: 1.25rem;
    }
    
    .section {
      padding: 1rem;
      margin: 1rem 0;
    }
    
    .section h2 {
      font-size: 1rem;
    }
    
    .founders-grid {
      grid-template-columns: 1fr;
    }
    
    .founder-card {
      padding: 1rem;
    }
    
    .founder-photo {
      width: 64px;
      height: 64px;
    }
    
    .offer-item {
      padding: 0.5rem;
      gap: 0.5rem;
    }
    
    .offer-icon {
      width: 28px;
      height: 28px;
      font-size: 0.75rem;
    }
  }

  /* ✅ Very Small Screens */
  @media (max-width: 360px) {
    .about-content-wrapper {
      padding: 0.75rem;
    }
    
    .about-hero h1 {
      font-size: 1.125rem;
    }
    
    .section {
      padding: 0.875rem;
    }
    
    .section-header {
      flex-direction: column;
      align-items: flex-start;
      gap: 0.5rem;
    }
    
    .offer-item {
      flex-direction: column;
      align-items: flex-start;
    }
    
    .founder-card h3 {
      font-size: 0.95rem;
    }
    
    .commitment {
      padding: 1rem;
    }
  }

  /* ✅ Tablet Optimization */
  @media (min-width: 481px) and (max-width: 768px) {
    .about-page {
      margin: 1rem;
    }
    
    .founders-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  /* ✅ Animation for Sections */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .section {
    animation: fadeInUp 0.5s ease-out;
  }

  .section:nth-child(2) { animation-delay: 0.1s; }
  .section:nth-child(3) { animation-delay: 0.2s; }
  .section:nth-child(4) { animation-delay: 0.3s; }
  .section:nth-child(5) { animation-delay: 0.4s; }
`;

const pageScripts = `
  document.addEventListener('DOMContentLoaded', function() {
    // Add smooth scroll for anchor links
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
      anchor.addEventListener('click', function(e) {
        e.preventDefault();
        const targetId = this.getAttribute('href');
        if (targetId === '#') return;
        
        const targetElement = document.querySelector(targetId);
        if (targetElement) {
          targetElement.scrollIntoView({
            behavior: 'smooth',
            block: 'start'
          });
        }
      });
    });
    
    // Add hover effect to founder cards
    const founderCards = document.querySelectorAll('.founder-card');
    founderCards.forEach(card => {
      card.addEventListener('mouseenter', () => {
        const photo = card.querySelector('.founder-photo');
        if (photo) {
          photo.style.transform = 'scale(1.05)';
          photo.style.transition = 'transform 0.3s ease';
        }
      });
      
      card.addEventListener('mouseleave', () => {
        const photo = card.querySelector('.founder-photo');
        if (photo) {
          photo.style.transform = 'scale(1)';
        }
      });
    });
    
    // Add section IDs for navigation
    const sections = document.querySelectorAll('.section h2');
    sections.forEach((section, index) => {
      const id = 'section-' + (index + 1);
      section.setAttribute('id', id);
      
      // Add anchor link icon
      const anchor = document.createElement('a');
      anchor.href = '#' + id;
      anchor.className = 'section-anchor';
      anchor.innerHTML = '🔗';
      anchor.style.marginLeft = '0.5rem';
      anchor.style.fontSize = '0.75rem';
      anchor.style.opacity = '0.5';
      anchor.style.textDecoration = 'none';
      anchor.style.transition = 'opacity 0.2s ease';
      
      anchor.addEventListener('mouseenter', () => {
        anchor.style.opacity = '1';
      });
      
      anchor.addEventListener('mouseleave', () => {
        anchor.style.opacity = '0.5';
      });
      
      section.appendChild(anchor);
    });
    
    // Highlight current section in view
    const observerOptions = {
      root: null,
      rootMargin: '0px',
      threshold: 0.2
    };
    
    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.style.boxShadow = '0 0 0 2px var(--green)';
          setTimeout(() => {
            entry.target.style.boxShadow = '';
          }, 2000);
        }
      });
    }, observerOptions);
    
    document.querySelectorAll('.section').forEach(section => {
      observer.observe(section);
    });
    
    // Mobile menu for table of contents (optional)
    const createMobileTOC = () => {
      if (window.innerWidth <= 480) {
        const toc = document.createElement('div');
        toc.className = 'mobile-toc';
        toc.innerHTML = \`
          <select class="toc-select" aria-label="Jump to section">
            <option value="">Quick navigation...</option>
            \${Array.from(sections).map(section => 
              \`<option value="\${section.id}">\${section.textContent.replace('🔗', '')}</option>\`
            ).join('')}
          </select>
        \`;
        
        const hero = document.querySelector('.about-hero');
        if (hero) {
          hero.appendChild(toc);
        }
        
        const select = toc.querySelector('.toc-select');
        select.addEventListener('change', function() {
          if (this.value) {
            const target = document.getElementById(this.value);
            if (target) {
              target.scrollIntoView({ behavior: 'smooth' });
              this.value = '';
            }
          }
        });
      }
    };
    
    createMobileTOC();
    window.addEventListener('resize', createMobileTOC);
  });
`;
%>

<!-- ✅ Inject Styles -->
<style nonce="<%= nonce %>">
  <%= pageStyles %>
</style>

<!-- ✅ About Page Content -->
<div class="about-page">
  <div class="about-content-wrapper">
    <!-- Hero Section -->
    <header class="about-hero">
      <div class="about-badge">
        <span>🌍</span>
        <span>Our Story</span>
      </div>
      <h1>About <strong>Unicoporate.com</strong></h1>
      <p>Empowering businesses and customers through a unified e-commerce ecosystem.</p>
    </header>

    <!-- Vision Section -->
    <section class="section" id="vision">
      <div class="section-header">
        <div class="section-icon">🎯</div>
        <h2>Our Vision</h2>
      </div>
      <p>
        At <strong>Unicoporate.com</strong>, we believe in a smarter, more connected world of commerce.
        Our mission is to simplify online trading by connecting buyers, sellers, and suppliers worldwide.
      </p>
      <p>
        We envision a future where every entrepreneur has equal access to global markets,
        powered by technology that works for everyone.
      </p>
    </section>

    <!-- Story Section -->
    <section class="section" id="story">
      <div class="section-header">
        <div class="section-icon">📖</div>
        <h2>Our Story</h2>
      </div>
      <p>
        Founded by <strong>Phakisi Lighten Moteane</strong> and <strong>Moteane Kabelo</strong>, 
        Unicoporate.com began as a vision to bridge the gap between small businesses and the digital economy.
      </p>
      <p>
        What started as a simple idea has grown into a full e-commerce ecosystem built on
        transparency, technology, and trust—helping businesses thrive in the digital age.
      </p>
    </section>

    <!-- Offerings Section -->
    <section class="section" id="offerings">
      <div class="section-header">
        <div class="section-icon">✨</div>
        <h2>What We Offer</h2>
      </div>
      <p>Our comprehensive platform provides everything you need to succeed online:</p>
      <ul class="offer-list">
        <li class="offer-item">
          <div class="offer-icon">🛍️</div>
          <div class="offer-text"><strong>Business Solutions</strong> – Complete dashboards for sellers, suppliers, and buyers.</div>
        </li>
        <li class="offer-item">
          <div class="offer-icon">💳</div>
          <div class="offer-text"><strong>Secure Payments</strong> – PayPal integration with end-to-end encryption.</div>
        </li>
        <li class="offer-item">
          <div class="offer-icon">🚚</div>
          <div class="offer-text"><strong>Smart Shipments</strong> – Real-time tracking and delivery coordination.</div>
        </li>
        <li class="offer-item">
          <div class="offer-icon">☁️</div>
          <div class="offer-text"><strong>Cloud Storage</strong> – Reliable product/image hosting via AWS S3.</div>
        </li>
        <li class="offer-item">
          <div class="offer-icon">💬</div>
          <div class="offer-text"><strong>Engagement Tools</strong> – Messaging and feedback systems for better communication.</div>
        </li>
      </ul>
    </section>

    <!-- Founders Section -->
    <section class="section" id="founders">
      <div class="section-header">
        <div class="section-icon">👥</div>
        <h2>Meet the Founders</h2>
      </div>
      <p>Our leadership team combines technical expertise with business vision:</p>
      
      <div class="founders-section">
        <div class="founders-grid">
          <!-- Founder Card -->
          <div class="founder-card" aria-label="Founder card">
            <img src="/images/products-images/founder.jpg" alt="Phakisi Lighten Moteane" 
                 class="founder-photo" width="72" height="72" loading="lazy" decoding="async"
                 onerror="this.src='https://ui-avatars.com/api/?name=PL+Moteane&background=22C55E&color=fff&size=72'">
            <h3>Phakisi Lighten Moteane</h3>
            <p class="role">Founder & Lead Developer</p>
            <p class="contact">Email: <a href="mailto:phakisingxongxela@gmail.com">phakisingxongxela@gmail.com</a></p>
            <a href="https://www.facebook.com/profile.php?id=100083397021296" target="_blank" 
               class="social-link" rel="noopener noreferrer">
              <span>📘</span> Facebook
            </a>
          </div>

          <!-- Co-Founder Card -->
          <div class="founder-card" aria-label="Co-Founder card">
            <img src="/images/products-images/co-founder.jpg" alt="Moteane Kabelo" 
                 class="founder-photo" width="72" height="72" loading="lazy" decoding="async"
                 onerror="this.src='https://ui-avatars.com/api/?name=M+Kabelo&background=7C3AED&color=fff&size=72'">
            <h3>Moteane Kabelo</h3>
            <p class="role">Co-Founder & Business Strategist</p>
            <p class="contact">Email: <a href="mailto:moteanekabelo37@gmail.com">moteanekabelo37@gmail.com</a></p>
            <a href="https://www.facebook.com/profile.php?id=100094603932435" target="_blank" 
               class="social-link" rel="noopener noreferrer">
              <span>📘</span> Facebook
            </a>
          </div>
        </div>
      </div>
    </section>

    <!-- Commitment Section -->
    <section class="section" id="commitment">
      <div class="section-header">
        <div class="section-icon">🤝</div>
        <h2>Our Commitment</h2>
      </div>
      <div class="commitment">
        <p>
          We build tools that empower African and global entrepreneurs to thrive in the digital marketplace—guided by innovation,
          inclusivity, and integrity.
        </p>
        <p>
          <strong>Our promise:</strong> To provide a platform where every business, regardless of size, can grow and succeed.
        </p>
      </div>
    </section>

    <!-- Footer -->
    <footer class="about-footer">
      <div class="footer-tagline">
        <span>💼</span>
        <strong>Unicoporate.com</strong> 
        <span>–</span>
        <span>Redefining e-commerce. Connecting the world.</span>
      </div>
    </footer>
  </div>
</div>

<!-- ✅ Inject Scripts -->
<script nonce="<%= nonce %>">
  <%= pageScripts %>
</script>